<Window x:Class="EmployeeSearch.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Employee Search — Login"
        Height="520" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#1B2A4A" Offset="0"/>
            <GradientStop Color="#2B5278" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <Grid>
        <!-- Card -->
        <Border Background="White" CornerRadius="14" Width="340"
                VerticalAlignment="Center" Padding="36,32">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30" ShadowDepth="8" Opacity="0.25" Color="#000000"/>
            </Border.Effect>

            <StackPanel>
                <!-- Icon -->
                <Border Background="#1B4F8A" Width="62" Height="62" CornerRadius="31"
                        HorizontalAlignment="Center" Margin="0,0,0,18">
                    <TextBlock Text="🔍" FontSize="26"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <TextBlock Text="Employee Search" FontSize="22" FontWeight="Bold"
                           Foreground="#1B2838" HorizontalAlignment="Center"
                           Margin="0,0,0,4"/>
                <TextBlock Text="Sign in to your account" FontSize="13"
                           Foreground="#6B7280" HorizontalAlignment="Center"
                           Margin="0,0,0,28"/>

                <!-- Username field -->
                <TextBlock Text="Username" FontSize="13" FontWeight="SemiBold"
                           Foreground="#374151" Margin="0,0,0,6"/>
                <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6"
                        Background="#F9FAFB">
                    <TextBox x:Name="UsernameBox" Height="40" FontSize="14"
                             Padding="10,0" VerticalContentAlignment="Center"
                             BorderThickness="0" Background="Transparent"
                             KeyDown="Input_KeyDown"/>
                </Border>

                <!-- Password field -->
                <TextBlock Text="Password" FontSize="13" FontWeight="SemiBold"
                           Foreground="#374151" Margin="0,14,0,6"/>
                <Border BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="6"
                        Background="#F9FAFB">
                    <PasswordBox x:Name="PasswordBox" Height="40" FontSize="14"
                                 Padding="10,0" VerticalContentAlignment="Center"
                                 BorderThickness="0" Background="Transparent"
                                 KeyDown="Input_KeyDown"/>
                </Border>

                <!-- Error message -->
                <Border x:Name="ErrorBorder" Background="#FEF2F2" CornerRadius="6"
                        Margin="0,12,0,0" Padding="10,8" Visibility="Collapsed">
                    <TextBlock x:Name="ErrorText" Foreground="#DC2626" FontSize="12"
                               TextWrapping="Wrap"/>
                </Border>

                <!-- Login button -->
                <Button x:Name="LoginButton" Content="Sign In"
                        Height="44" Margin="0,22,0,0"
                        Foreground="White" FontSize="15" FontWeight="SemiBold"
                        BorderThickness="0" Cursor="Hand"
                        Click="LoginButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#1B4F8A"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="7">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                              VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#154080"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#0F3060"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <!-- Hint -->
                <TextBlock FontSize="11" Foreground="#9CA3AF"
                           HorizontalAlignment="Center" Margin="0,16,0,0">
                    <Run Text="Default credentials: "/>
                    <Run Text="admin" FontWeight="Bold" Foreground="#6B7280"/>
                    <Run Text=" / "/>
                    <Run Text="admin123" FontWeight="Bold" Foreground="#6B7280"/>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</Window>
